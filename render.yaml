services:
  - type: web
    name: bill-downloader-backend
    env: python
    buildCommand: "pip install -r requirements.txt && playwright install chromium"
    startCommand: "python backend.py"
    plan: free